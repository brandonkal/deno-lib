{
  "deno.enable": false,
  "runOnSave.commands": [
    {
      "command": "deno fmt ${file} && deno test -A -f yaml.test.ts",
      "runIn": "terminal"
    }
    // {
    //   "command": "deno fmt ${file} && deno test -A -f dedent.test.ts",
    //   "runIn": "terminal"
    // }
  ]
}
